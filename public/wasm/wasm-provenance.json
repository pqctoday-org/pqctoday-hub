{
  "_comment": "Provenance for WASM bundles built from the pqctoday-hsm repo and vendored into this hub. When you rebuild a bundle with its hsm build script, update its `hsmCommit`/`builtAt`. Verify drift with `npm run sync:wasm:check` (compares against a sibling ../pqctoday-hsm checkout when present; no-op otherwise). Bundles built from other sources (openssl upstream, pqctoday-tpm) are intentionally omitted.",
  "hsmRepo": "pqctoday-org/pqctoday-hsm",
  "bundles": [
    {
      "name": "cacp-kmip",
      "files": ["public/wasm/rust-kmip/pqctoday_kmip_wasm_bg.wasm", "src/wasm/kmip/"],
      "buildScript": "scripts/build-kmip-wasm.sh",
      "sourceDirs": ["kmip", "wasm"],
      "track": "tip",
      "status": "built",
      "hsmCommit": "3480bec2dcfb8ce091a3ce463f4f2547a36d7056",
      "builtAt": "2026-08-08",
      "_buildNote": "Rebuilt 2026-08-08 from hsm main @ 3480bec (v0.21.0) via scripts/build-kmip-wasm.sh, closing a 12-commit drift over kmip/ and wasm/. The script's node smoke test passed end-to-end IN WASM: Query -> Success, Create (real keygen), a deliberately malformed frame answered without an exception, CreateKeyPair ML-DSA-65 -> Sign (3309 bytes) -> SignatureVerify = Valid, an unimplemented algorithm (Falcon-512) rejected with the keystore left unchanged, and a full X25519MLKEM768 hybrid-KEM encapsulate/decapsulate round trip. Scope note: v0.21.0's headline KMIP work is the SERVER's quantum-safe TLS profile (--tls-profile) and the Python client, neither of which is reachable from this wasm bundle -- it has no TLS listener and no socket. What IS reachable and new here is SignatureVerify, which the smoke test exercises above. Prior entry: Rebuilt 2026-08-02 from hsm main @ 8386717 (v0.20.2) via scripts/build-kmip-wasm.sh with HUB= pointed at the remediation worktree (the script stages straight into a sibling pqctoday-hub by default — override it or it writes into whatever branch the main checkout happens to be on). Closes a 2-commit drift (v0.17.0 9f618c4 + v0.16.0 4f08746). The script's own node smoke test passed end-to-end in was"
    },
    {
      "name": "softhsmrustv3-engine",
      "files": ["public/wasm/rust/softhsmrustv3_bg.wasm", "src/wasm/softhsmrustv3_bg.wasm"],
      "buildScript": "rust/build-wasm-bundle.sh",
      "sourceDirs": ["rust"],
      "track": "tip",
      "status": "built",
      "hsmCommit": "3e2f5055b",
      "builtAt": "2026-08-09",
      "_buildNote": "Rebuilt 2026-08-09 from hsm origin/main @ 3e2f5055b, closing the ONE-commit drift the corrected provenance gate reported: d07dfa9, 'RSA private keys must expose CKA_MODULUS and CKA_PUBLIC_EXPONENT' (+87 lines in rust/src/ffi.rs). Built in a dedicated worktree at origin/main so the main hsm checkout, which sits on feat/kmip-bench-followups, was never disturbed. Verified d07dfa9 is an ancestor of the build tree rather than assuming it. IMPORTANT context for the next reader: before the gate was fixed the same day it reported THREE bundles stale, because it compared against hsm's checked-out HEAD rather than main — the extra two were bench-harness commits, and rebuilding on that signal would have baked bench code into shipped wasm. Artifact diff vs the shipped bundle: 2,916,599 -> 2,916,703 bytes (+104), export surface unchanged at 112 distinct C_* symbols (same set, no additions or removals). The wasm-bindgen glue changed only in parameter naming (_h_session -> h_session and similar), because the fix gave names to parameters that had been ignored — the same glue-churn signature the 2026-08-08 rebuild note describes. PKCS#11 attribute constants are numeric, so CKA_MODULUS does not appear as a string in the binary; absence there is not evidence and the fix was confirmed from the source tree instead.",
      "_priorBuildNote20260808": "Rebuilt 2026-08-08 from hsm main @ 3480bec (v0.21.0) via rust/build-wasm-bundle.sh, closing a 6-commit drift over rust/. No functional change reachable from this target: the v0.21.0 rust/ work is the operation-evidence log, and its sink is cfg-gated OUT on wasm32-unknown-unknown (see the 'WASM targets must keep building' note in rust/src/oplog.rs), which is why no PQCEV string appears in the bundl"
    },
    {
      "name": "softhsm-cpp-engine",
      "files": ["public/wasm/softhsm.wasm", "public/wasm/libsofthsmv3.wasm"],
      "buildScript": "scripts/build-wasm.sh",
      "sourceDirs": ["src/lib"],
      "track": "tip",
      "status": "built",
      "hsmCommit": "3480bec2dcfb8ce091a3ce463f4f2547a36d7056",
      "builtAt": "2026-08-08",
      "_buildNote": "Rebuilt 2026-08-08 from hsm main @ 3480bec (v0.21.0) via scripts/build-wasm.sh with emsdk 6.0.4 (SKIP_OPENSSL=1 -- deps/openssl-wasm was already current and is not touched by this bump), closing a 1-commit drift over src/lib. That commit is the C++ operation-evidence log; it is runtime-gated on SOFTHSM3_OP_LOG, which nothing sets in the browser, so the shipped behaviour is unchanged. node tests/smoke-wasm.mjs PASSED against this exact binary. Export surface unchanged at 104 distinct C_* symbols. The glue shrank 139290 -> 91013 bytes on the newer emsdk; that is codegen, not lost exports. Both PAIRS are staged consistently -- softhsm.{js,wasm} from wasm/ and libsofthsmv3.{js,wasm} from build-wasm/src/lib/ -- and the two .wasm are byte-identical to each other, as they were before. Prior entry: Rebuilt 2026-08-02 from hsm main @ 8386717 (v0.20.2) via scripts/build-wasm.sh, closing a 5-commit drift. Pulls in ECDH-as-KEM under C_EncapsulateKey/C_DecapsulateKey (a450d42), the CKM_ECDH1_COFACTOR_DERIVE-vs-CKK_EC_MONTGOMERY rejection (6270b73), the v0.19.0 emsdk 6.0.4 / OpenSSL 3.6.3 toolchain fixes (e85b26d), and the Split Key secret-sharing math (4cc4c4b). Built with emsdk 6.0.4 — NOTE for "
    },
    {
      "name": "openssh-pkcs11",
      "files": ["public/wasm/openssh-server.js", "public/wasm/openssh-server.wasm"],
      "buildScript": "openssh-pkcs11/scripts/build-wasm.sh",
      "sourceDirs": ["openssh-pkcs11"],
      "track": "tip",
      "status": "built",
      "hsmCommit": "3480bec2dcfb8ce091a3ce463f4f2547a36d7056",
      "builtAt": "2026-08-08",
      "_buildNote": "Rebuilt 2026-08-08 from hsm main @ 3480bec (v0.21.0) via openssh-pkcs11/scripts/build-wasm.sh with emsdk 6.0.4, closing a 2-commit drift. Those two commits are the apply_mldsa_patches.py anchor fix and its --dry-run preflight; the build still pins OPENSSH_VERSION=V_10_3_P1, and on 10.3 there are no upstream-inserted key types for the widened anchor to preserve, so the patched source is unchanged. openssh-server.js came out byte-identical; only openssh-server.wasm moved, because it statically links the freshly rebuilt libsofthsmv3-static.a. Built from a PRISTINE re-fetched tree: SKIP_OPENSSH_FETCH=1 fails on an already-patched tree ('marker not found in Makefile.in'), which is the patcher refusing to double-apply. Only the SHIPPED pair is staged -- copy-to-hub.sh also copies openssh-client.{js,wasm}, which the hub has never tracked, so those were removed again to keep the diff faithful to what actually ships. Prior entry: Genuinely rebuilt 2026-08-02 from hsm main @ 8386717 (v0.20.2) via openssh-pkcs11/scripts/build-wasm.sh with emsdk 6.0.4, closing a 5-commit drift. Pulls in v0.20.0 (13bca12, SLH-DSA-SHA2-128s SSH auth runtime-verified end-to-end), the SLH-DSA backport (dcdbed8), and the v0.19.0 emsdk 6.0.4 / OpenSSL 3.6.3 toolchain fixes (e85b26d). Only the SHIPPED pair is staged — public/wasm/openssh-server.{js,"
    },
    {
      "name": "strongswan",
      "files": ["public/wasm/strongswan.wasm", "public/wasm/strongswan-v2.wasm"],
      "buildScript": "scripts/build-strongswan-wasm.sh",
      "sourceDirs": ["strongswan-wasm-shims", "strongswan-wasm-v2-shims"],
      "track": "tip",
      "status": "built",
      "hsmCommit": "1c637d3a3916b08fe59445120d93f26dfd79d639",
      "builtAt": "2026-07-10",
      "_buildNote": "Re-stamped, not rebuilt: verified `git log 8e436210c..1c637d3 -- strongswan-wasm-shims/ strongswan-wasm-v2-shims/` touches only STATUS.md and README.md (1 doc-only commit, zero source/behavior change). No emsdk/Emscripten toolchain is installed on this machine, so an actual rebuild wasn't attempted or needed — the existing binary is bit-for-bit what v0.14.0's source would produce."
    },
    {
      "name": "openssl-pkcs11",
      "files": ["public/wasm/openssl.js", "public/wasm/openssl.wasm"],
      "buildScript": "pqctoday-priv/build-wasm.sh",
      "sourceDirs": ["rust", "src/vendor/pkcs11-provider", "scripts/build-openssl-wasm.sh"],
      "track": "tip",
      "status": "built",
      "hsmCommit": "3e2f5055be9d3760e02ab0128f831c89176fd97f",
      "builtAt": "2026-08-09",
      "_rebuild20260807": "Rebuilt 2026-08-07 from hsm main @ 67f739f (merge of PR #153, feat/rust-pkcs11-emscripten-staticlib), closing a 3-commit drift — the same feature branch this bundle's _priorBuild/_buildNote below already describe now landed on main via a clean merge (hsm main's rust/ tree after the merge is identical to 859774b's, confirmed via `git log --stat`). NOTE: the hsmCommit given in the original task request (67f739fc3916b08fe59445120d93f26dfd79d639) does not exist in the hsm repo — corrected here to the actual merge-commit HEAD (67f739fc331cfdb1406197583cf6432f52833080), verified via `git rev-parse HEAD` in pqctoday-hsm. Rebuilt fresh end-to-end per the established process: scripts/build-pkcs11-provider-wasm.sh prerequisite (idempotent — archive already current), a fresh wasm32-unknown-emscripten Rust staticlib build with --features acvp (superseding the stale 2026-07-24 archive), OpenSSL 3.6.3 source re-verified against the pinned sha256 (243a86649cf6f23eeb6a2ff2456e09e5d77dd9018a54d3d96b0c6bdd6ba6c7f1) before extracting, then pqctoday-priv/build-wasm.sh with HUB_ROOT/HSM_ROOT/OPENSSL_SRC set explicitly. Since the underlying rust/ tree is unchanged from the last build, the resulting binary is FUNCTIONALLY IDENTICAL to the previous one — this rebuild is provenance/timestamp hygiene, not a behavior change. VERIFIED: `openssl version` reports 3.6.3; all custom exports present (_pqctoday_cms_init, _pqctoday_composite_mkcert, _execute_cmp_simulation, PKCS#11 C_* symbols); 17/17 real ML-DSA/SLH-DSA/ML-KEM cross-implementation KATs (Node 24 crypto vs this WASM binary) pass; 72/72 in the rest of the OpenSSL Studio + anchor-KAT suites (89 total, 0 failed); `npx tsc -b` clean.",
      "_rebuild20260802": "Rebuilt 2026-08-02 from hsm main @ 8386717 (v0.20.2), closing a 6-commit drift AND moving the bundled OpenSSL from 3.6.2 to 3.6.3 (deliberate, user-approved — hsm's deps/openssl-wasm had already moved to 3.6.3 in v0.19.0, so the bundle was behind on both axes). Source: openssl-3.6.3.tar.gz from the official GitHub release, sha256 verified against the published checksum (243a86649cf6f23eeb6a2ff2456e09e5d77dd9018a54d3d96b0c6bdd6ba6c7f1) before extracting. Build needs a prerequisite the main script does not build itself: scripts/build-pkcs11-provider-wasm.sh must run first to produce build-wasm/lib/libpkcs11-provider.a, otherwise build-wasm.sh exits immediately with 'missing pkcs11-provider archive'. Invoked as: HUB_ROOT=<hub checkout> HSM_ROOT=<hsm> OPENSSL_SRC=<openssl-3.6.3 tree> bash pqctoday-priv/build-wasm.sh (HUB_ROOT must be set explicitly — the script's location-derived default is wrong since it moved into pqctoday-priv). The 'apps/openssl link failed (undefined symbol: pqctoday_cms_init)' line in the log is EXPECTED and handled — the script continues to its own custom emcc link; do not read it as a build failure. VERIFIED: `openssl version` now reports 3.6.3 (via the real driver harness, not assumed); OpenSSL Studio suites 57/57 main + 32/32 local incl. the ML-DSA/ML-KEM cross-implementation and anchor KATs, all of which re-confirm real crypto outputs against this new binary; and — closing the prior entry's explicit 'NOT YET VERIFIED: a full pass in an actual browser' gap — e2e/cms-hsm-sign.spec.ts 5/5 in real chromium, including T3 routing an ML-DSA-65 CMS signature through pkcs11-provider into softhsmv3 with 'HSM (pkcs11:)' / 'resident in softhsmv3' evidence. Version references across the Studio's UI copy, lessons, glossary and KAT comments were updated 3.6.2 -> 3.6.3; the only 3.6.2 mentions left are dated historical records of when content was first captured. STILL UNCONFIRMED (carried over): whether the 'HRR detection' patch referenced by the build script's banner is present in this tree.",
      "_priorBuild": "859774b5273869c256f604a73eafcc8ccc8ee80c / 2026-07-24",
      "_buildNote": "REBUILT 2026-08-09 from hsm main @ 3e2f5055b to carry the RSA private-key attribute fix (hsm PR #155): PKCS#11 v3.2 requires CKA_MODULUS and CKA_PUBLIC_EXPONENT on the RSA PRIVATE key object, and the engine set them on the public key only. pkcs11-provider's fetch_rsa_key() requests both with required=true, so the fetch failed and the optional CKA_ID was never populated on the provider's cached object -- surfacing far away as \"No CKA_ID in source object\" (objects.c:1646) from p11prov_obj_export_public_rsa_key (objects.c:2229). Only RSA is affected: EC and ML-DSA public keys are derivable from their private half, so the provider never resolves a sibling. Verified END TO END, not just at the unit level -- e2e/cms-workshop-crypto.spec.ts 13 passed, with the previously-red composite reporting: composite id-MLDSA44-RSA2048-PSS-SHA256 PASS oid=1.3.6.1.5.5.7.6.37 mldsa=2420B classical=RSA-2048-PSS. Engine suite 327 passed; the regression test was confirmed to FAIL without the fix before being accepted. Prior entry: Rebuilt 2026-08-08 from hsm main @ 3480bec (v0.21.0) via pqctoday-priv/build-wasm.sh, closing a 6-commit drift over rust/, src/vendor/pkcs11-provider and scripts/build-openssl-wasm.sh. The linked PKCS#11 module is a freshly built softhsmrustv3 wasm32-unknown-emscripten staticlib from the same commit  MERGE NOTE 2026-08-09 (v4.45.0): two sessions independently rebuilt this bundle from the same hsm commit 3e2f5055b on the same day. Both builds are real and carry the same fix; they differ only in Emscripten emission order and size (6,915,010 vs 6,925,466 bytes), the usual non-determinism. This entry and the .wasm/.js pair on disk are the origin/main build (hsm PR #155 via hub PR #508), kept because it is the artifact already deployed in 4.44.1 and the one verified end to end. The alternate build was discarded rather than mixed -- glue and binary must ship as a matched pair.",
      "hsmBranch": "feat/rust-pkcs11-emscripten-staticlib (NOT merged to hsm main as of this build)"
    }
  ]
}
